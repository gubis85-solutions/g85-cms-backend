// * import type { Core } from '@strapi/strapi';

export default {
  // * Runs before application initialization and can be used to extend code.
  register(/* { strapi }: { strapi: Core.Strapi } */) {},

  // * Runs before application startup and can initialize data/models/jobs.
  bootstrap(/* { strapi }: { strapi: Core.Strapi } */) {},
};
